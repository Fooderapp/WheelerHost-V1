# Requirements for Windows - Numpy Compatibility Fixed

# Core dependencies
PySide6>=6.5.0
requests>=2.28.0

# ONNX Runtime for sound event detection
onnxruntime>=1.15.0

# Audio capture (Windows WASAPI loopback, macOS/Linux input)
sounddevice>=0.4.6

# Numpy - now with proper version pinning for Windows compatibility
# If this still fails, Wheeler will use pure Python fallbacks automatically
numpy>=1.24.0,<2.0.0

# Optional: For enhanced audio processing
scipy>=1.10.0

# Network utilities
netifaces>=0.11.0

# Windows-specific: Enhanced gamepad support
# pywin32>=306  # Uncomment if using Windows-only features

# Note: If numpy installation fails on Windows:
# 1. Update pip: python -m pip install --upgrade pip
# 2. Install Microsoft Visual C++ Build Tools
# 3. Or use: pip install numpy --only-binary=numpy
# 4. Wheeler will automatically fall back to pure Python if numpy is unavailable